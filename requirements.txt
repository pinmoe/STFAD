# =============================================================================
# Anomaly-Transformer 项目依赖
# 生成日期: 2026-06-13
# =============================================================================
# 核心框架
torch>=1.8.0
numpy>=1.19.0

# 数据处理
pandas>=1.2.0
scikit-learn>=0.24.0

# 图像处理 (data_loader 中 import PIL，主要用于 PSM 等数据集)
Pillow>=8.0.0

# 日志模块 (utils/logger.py 使用，属于遗留依赖，主流程不依赖)
tensorflow>=2.4.0
scipy>=1.6.0
